#!/usr/bin/env bash
# Minimal stub gradlew to satisfy CI environments that attempt to invoke Gradle.
# This does not perform a real Android build. It simply exits success for common commands.
# For real native builds, replace this stub with a proper Android project generated by `expo prebuild --platform android`.
echo "Stub gradlew called with args: $@"
exit 0
